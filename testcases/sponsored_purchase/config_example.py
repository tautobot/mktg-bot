#TODO: Move all config values to root project to config for all features
#gmail account for check email
email='email@gigacover.com'
password='test@1234'

#emulator android device name
device_name='emulator-device-name'

#selenium running mode, fill 'yes' if u wanna run on headless , ortherwise fill 'no'
headless='yes'

#ssh on host: aqa will be run on host which is found (available and valuable) and order by docker -> release -> staging
docker=""
release=""
staging="autoarmour@ip"
